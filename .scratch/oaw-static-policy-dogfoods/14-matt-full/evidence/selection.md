# MATT-FULL Selection Evidence

## Isolated run

- Temporary project: `/tmp/oaw-matt-full.mxJ4P7/project`
- Isolated `HOME`, `XDG_CONFIG_HOME`, and `XDG_STATE_HOME`: under
  `/tmp/oaw-matt-full.mxJ4P7/`
- Installation: project scope, Codex target only
- Assurance: `policy-cooperative`; no Assurance or Bridge executable was
  installed
- Topology: current Host session only

## Incomplete index, readable Skill resolution

The fixture `host-skills-index.txt` listed `implement`, `tdd`, and
`code-review`, but did not list the Matt framing entry. The model still read
the current Host's readable `grill-with-docs/SKILL.md` and its referenced
domain-modeling rules. The managed `.oaw/policy/profiles/MATT-FULL.md` assigned
that entry to Problem framing and assigned `to-spec`, `to-tickets`, `implement`,
and `tdd` to the remaining responsibilities.

The activated request was:

```text
Use OAW with MATT-FULL to deliver a checklist summary command for this release
document. Use the readable Matt rules even if the Host index is incomplete.
```

No route-admission failure, repeated Profile/topology/add-on confirmation, or
Provider identity claim occurred. The only limitation was the normal
Policy-cooperative boundary.
