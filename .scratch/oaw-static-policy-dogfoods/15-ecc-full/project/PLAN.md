# Delivery Plan

This ordinary single-delivery task uses the ECC-FULL Policy Default for
Delivery planning. `ecc:blueprint` was read but not invoked because its own
semantic trigger excludes work that fits in one small delivery.

1. Specify bucket and selection behavior with unit tests, observe RED, and add
   the minimal implementation.
2. Specify the CLI adapter with tests, observe RED, and add argument parsing and
   output.
3. Run coverage, review the committed result, and perform fresh verification
   after removing the installer executable.
