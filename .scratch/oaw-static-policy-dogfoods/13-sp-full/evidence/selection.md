# SP-FULL Selection Evidence

## Isolated run

- Temporary project: `/tmp/oaw-sp-full.1yKmHN/project`
- Isolated `HOME`, `XDG_CONFIG_HOME`, and `XDG_STATE_HOME`: under
  `/tmp/oaw-sp-full.1yKmHN/`
- Installation: project scope, Codex target only
- Assurance: `policy-cooperative`; no Assurance or Bridge executable was
  installed
- Topology: current Host session only

## Natural-language selection

The activated request was:

```text
Use OAW with SP-FULL to deliver a slugify command for release headings.
Keep the work in this project and use the Skills already readable in the Host.
```

The project router loaded `.oaw/policy/POLICY.md`; the selected managed Profile
was `.oaw/policy/profiles/SP-FULL.md`. Its Responsibility table was readable
after the `oaw` binary was removed and named the Superpowers rules for
brainstorming, planning, TDD, execution, review, verification, and closeout.

The only limitation was the normal Policy boundary: no Provider identity,
Bridge evidence, machine lifecycle state, or enforced recovery was claimed.
No topology/add-on/limitation confirmation form was emitted.

## Scope proof

The project committed the Policy Set and artifact at `abe6180`. No user Policy
Set was installed, and the isolated state root contained only the project
Install State record.
